{
  "name": "brave-search",
  "description": "Web search via Brave for research during coding sessions.",
  "config": {
    "command": "npx",
    "args": [
      "@modelcontextprotocol/server-brave-search@0.6.2"
    ],
    "env": {
      "BRAVE_API_KEY": "${BRAVE_API_KEY}"
    }
  }
}
