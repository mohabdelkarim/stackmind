{
  "name": "memory",
  "description": "Long-lived vectorized project memory for the FastAPI stack.",
  "config": {
    "command": "npx",
    "args": [
      "@modelcontextprotocol/server-memory"
    ]
  }
}
