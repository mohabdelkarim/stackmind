{
  "name": "github",
  "description": "GitHub repository, issues, and pull request access for FastAPI services.",
  "config": {
    "command": "npx",
    "args": [
      "@modelcontextprotocol/server-github@2025.4.8"
    ],
    "env": {
      "GITHUB_TOKEN": "${GITHUB_TOKEN}"
    }
  }
}
