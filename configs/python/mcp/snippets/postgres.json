{
  "name": "postgres",
  "description": "Async PostgreSQL access via SQLAlchemy-compatible MCP server.",
  "config": {
    "command": "npx",
    "args": [
      "@modelcontextprotocol/server-postgres@0.6.2"
    ],
    "env": {
      "POSTGRES_DSN": "${POSTGRES_DSN}"
    }
  }
}
