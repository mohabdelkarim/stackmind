{
  "name": "postgres",
  "description": "PostgreSQL access for FastAPI backends.",
  "config": {
    "command": "npx",
    "args": [
      "@modelcontextprotocol/server-postgres@0.6.2"
    ],
    "env": {
      "POSTGRES_DSN": "${POSTGRES_DSN}"
    }
  }
}
